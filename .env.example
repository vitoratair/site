# Optional: override the FormSubmit recipient (defaults to the email in src/locales/pt.ts)
# VITE_CONTACT_EMAIL=you@example.com

# Deploy Locaweb (make publish): copy .env.locaweb.example → .env.locaweb — not committed
